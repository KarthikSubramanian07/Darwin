# Darwin — Python runtime deps
# NOTE (Claude Code / build): pin exact versions once the sponsor SDK
# surfaces are VERIFIED against current docs. Do not invent method names.

python-dotenv>=1.0.0

# Sponsor SDKs — VERIFY install name + import surface before writing calls
daytona            # Daytona: parallel sandboxes + snapshot/restore  (verified: pip install daytona)
braintrust         # Braintrust: init()/Experiment.log()/summarize() (verified 2026-07)
autoevals          # Braintrust scorers (ExactMatch, Levenshtein, LLM-judge); optional at runtime
openai>=1.30.0     # Fireworks is OpenAI-compatible; set base_url     (VERIFY base_url + model ids)

# Server / plumbing
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.6.0
httpx>=0.27.0

# Dev / test (also see pyproject.toml)
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0
ruff>=0.4.0
